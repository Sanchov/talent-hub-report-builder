import { ControlToFormGroupPipe } from './control-to-form-group.pipe';

describe('ControlToFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlToFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
